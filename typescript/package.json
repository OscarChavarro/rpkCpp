{
  "name": "rpk-typescript-workspace",
  "version": "0.7.0",
  "private": true,
  "workspaces": [
    "base",
    "testsuite/ApplicationCases/RenderparkApplication"
  ],
  "scripts": {
    "build": "npm run -w vitral build && npm run -w renderpark-application build",
    "run": "npm run -w renderpark-application run",
    "run:help": "npm run -w renderpark-application run:help",
    "clean_all": "bash scripts/clean_all.sh"
  }
}
